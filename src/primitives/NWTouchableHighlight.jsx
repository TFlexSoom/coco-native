/**
 * Styled Nativewind Touchable Opacity Component
 */

import { styled } from 'nativewind';
import {
    TouchableHighlight
} from 'react-native';

const NWTouchableHighlight = styled(TouchableHighlight);
export default NWTouchableHighlight;