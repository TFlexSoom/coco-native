/**
 * Styled Nativewind ScrollView Component
 */

import { styled } from 'nativewind';
import {
    ScrollView
} from 'react-native';

const NWScrollView = styled(ScrollView);
export default NWScrollView;