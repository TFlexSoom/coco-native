/**
 * Styled Nativewind Touchable Opacity Component
 */

import { styled } from 'nativewind';
import {
    TouchableOpacity
} from 'react-native';

const NWTouchableOpacity = styled(TouchableOpacity);
export default NWTouchableOpacity;