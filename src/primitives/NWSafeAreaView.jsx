/**
 * Styled Nativewind SafeAreaView Component
 */

import { styled } from 'nativewind';
import {
    SafeAreaView
} from 'react-native';

const NWSafeAreaView = styled(SafeAreaView);
export default NWSafeAreaView;