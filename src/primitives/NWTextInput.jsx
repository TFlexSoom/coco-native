/**
 * Styled Nativewind TextInput Component
 */

import { styled } from 'nativewind';
import {
    TextInput
} from 'react-native';

const NWTextInput = styled(TextInput);
export default NWTextInput;